import React from "react";

export default function Header(props) {
  //   const { onClick } = props;
  return (
    <header>
      <h1>Omamori</h1>
      <button>UPLOAD</button>
    </header>
  );
}
